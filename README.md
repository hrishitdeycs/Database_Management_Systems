```sql
SHOW DATABASES; 
CREATE DATABASE College;
SHOW DATABASES; 
USE College;
SHOW TABLES;
-- Create STUDENT table
CREATE TABLE STUDENT (
    RollNo CHAR(6) PRIMARY KEY,
    StudentName VARCHAR(20) NOT NULL,
    Course VARCHAR(10),
    DOB DATE
);
-- Create SOCIETY table
CREATE TABLE SOCIETY (
    SocID CHAR(6) PRIMARY KEY,
    SocName VARCHAR(20) NOT NULL,
    MentorName VARCHAR(15),
    TotalSeats INT UNSIGNED
);
-- Create ENROLLMENT table
CREATE TABLE ENROLLMENT (
    RollNo CHAR(6),
    SID CHAR(6),
    DateOfEnrollment DATE,
    PRIMARY KEY (RollNo, SID),
    FOREIGN KEY (RollNo) REFERENCES STUDENT(RollNo),
    FOREIGN KEY (SID) REFERENCES SOCIETY(SocID)
);
SHOW TABLES;
DESC STUDENT;
DESC SOCIETY;
DESC ENROLLMENT;
INSERT INTO STUDENT (RollNo, StudentName, Course, DOB)
VALUES
('S001', 'Alice', 'BCA', '2004-01-15'),
('S002', 'Bob', 'BCom', '2003-08-20'),
('S003', 'Charlie', 'BSc', '2004-05-10');
SELECT * FROM STUDENT;
INSERT INTO SOCIETY (SocID, SocName, MentorName, TotalSeats)
VALUES
('SOC01', 'Drama Club', 'Mr. Sharma', 50),
('SOC02', 'Robotics', 'Ms. Kapoor', 40),
('SOC03', 'Debate Team', 'Dr. Mehta', 35);
SELECT * FROM SOCIETY;
INSERT INTO ENROLLMENT (RollNo, SID, DateOfEnrollment)
VALUES
('S001', 'SOC01', '2026-01-10'),
('S002', 'SOC02', '2026-01-11'),
('S003', 'SOC03', '2026-01-12');
SELECT * FROM ENROLLMENT;
```
